<main class="content-login">
  <div class="info col">
    <%= image_tag("logo_yohan.png", alt: "Yohan WMS") %>
    <h1>Sistema WMS</h1>
  </div>
  <div class="login col">
    <h2>Acesse aqui!</h2>
    <div class="error">Seu email ou senha estão incorretos.</div>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin", id: "new_user", role: "form"}) do |f| %>

      <div class="form-item">
        <%= f.email_field :email, class: "form-control", placeholder: "Email address", required: true, autofocus: true %>
      </div>

      <div class="form-item">
        <%= f.password_field :password, class: "form-control", placeholder: "Senha", required: true, autocomplete: false %>
      </div>

      <small class="msg">Para continuar, efetue login ou registre-se.</small>
      <div class="form-item">
         <%= f.submit "Efetuar Login", class: "submit", data: { disable_with: false }  %>
      </div>

      <div class="form-group">
        <%= link_to("Forgot Password", new_user_password_path) %>
      </div>

    <% end %>
  </div>
</main>
