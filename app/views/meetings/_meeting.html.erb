<tr>
  <td><%= date_br(meeting.date_event) %></td>
  <td><%= time_br(meeting.time_event) %></td>
  <td><%= meeting.local %></td>
  <td><%= meeting.summoned %></td>
  <td><%= meeting.facilitator %></td>
  <td><%= meeting.secretary %></td>
  <td><%= link_to 'Show', meeting, class: "btn btn-info btn-sm" %></td>
  <td><%= link_to 'Edit', edit_meeting_path(meeting), class: "btn btn-blue btn-sm" %></td>
</tr>