<fieldset class="item">

  <%#= f.label :driver_id, "Motorista" %>
  <%= f.collection_select :driver_id, Driver.all, :id, :nome,  {:prompt => 'Select'} %>

  <a href="#" class="remove">remove</a>

  <%= f.hidden_field :id %>
  <%= f.hidden_field :_destroy %>
</fieldset>
