<fieldset class="item">

  <%= f.label :vehicle_id, "Placa" %>
  <%= f.collection_select :vehicle_id, Vehicle.all, :id, :placa,  {:prompt => 'Select'} %>

  <a href="#" class="remove">remove</a>

  <%= f.hidden_field :id %>
  <%= f.hidden_field :_destroy %>
</fieldset>
