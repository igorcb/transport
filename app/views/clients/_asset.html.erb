<% if f.object.new_record? %>

<fieldset class="item">

  <%= f.file_field :asset %>

  <a href="#" class="remove" data-confirm="Are you sure?">remove</a>

  <%= f.hidden_field :id %>
  <%= f.hidden_field :_destroy %>
</fieldset>

<% end %>