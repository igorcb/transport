<fieldset class="item">

  <%#= f.label :activity_id, "" %>
  <%= f.collection_select :activity_id, Activity.all, :id, :descricao,  {:prompt => 'Select'} %>

  <a href="#" class="remove">remove</a>

  <%= f.hidden_field :id %>
  <%= f.hidden_field :_destroy %>
</fieldset>
