<% if object.cte_keys.present? %>

  <ul class="list-group">
    <% object.cte_keys.each do |item| %>
      <li class="list-group-item" > 
        <strong> Nfe:</strong> <%= item.cte %> - 
        <strong>Chave:</strong> <%= item.chave %> 
      </li>
    <% end %>
  </ul>
<% else %>  
  <h2 class="panel-title"> is not exist nfe association </h2>
<% end %>
