<%
series = [
    {name: "Entradas", data: @carriers}
  ]
options = {
  subtitle: 'Agrupados por Transportadoras',
  xtitle: 'Nº de entradas',
  ytitle: 'Transportadoras',
  stacked: false
}
 %>



 <div class="card">
   <h3 class="text-center">Entradas por transportadoras</h3>
   <div id="input" class="card-body">
     <div class="row">
       <div class="col-md-12">
         <h1 class="total"><span class="counter_number" data-init="0" data-end="<%= InputControl.available_supervisor.count %>"></span></h1>
         <%#= scatter_chart(first[:series], {**first[:options], theme: 'palette3'}) %>
         <%= bar_chart(series, {**options, height: (@carriers.count*30)+120, theme: 'palette5'}) %>
       </div>
   </div>
   <small>Atualizado hoje <%= date_br(Date.today) %> as <%= time_br(Time.now) %></small>
   </div>
 </div>
