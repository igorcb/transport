<%

series = [
    {name: "Entradas", data: @inputs_month  },
    {name: "Embarques", data: @boardings_month  }
  ]
options = {
  subtitle: 'Agrupados por dia',
  xtitle: 'dias',
  ytitle: 'Nº de entradas',
  stacked: true
}
 %>

 <div class="card">
   <h3 class="text-center">Entradas diárias</h3>
   <div id="input" class="card-body">
     <div class="row">
       <div class="col-md-12">
         <h1 class="total"><span class="counter_number" data-init="0" data-end="<%= InputControl.available_supervisor.count %>"></span></h1>
         <%= line_chart(series, options) %>
       </div>
     </div>
   <small>Atualizado hoje <%= date_br(Date.today) %> as <%= time_br(Time.now) %></small>
   </div>
 </div>
