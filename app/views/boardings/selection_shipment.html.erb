<style type="text/css">
	td div { width:76px; overflow: hidden; }
</style>
<h1>Gerar Embarque</h1>


<div id="ordem_service_type_service_id">
  <%= form_tag({:controller => "boardings", :action => "create"}, :method => "post", :name => "boardingForm") do %>

  <div class="panel panel-default">
    <div class="panel-heading">Selecione as OS para Embarque</div>
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table table-striped">
          <thead>
            <tr>
              <th></th>
              <th>No O.S.</th>
              <th>Client</th>
              <th>Cidade</th>
              <th>Estado</th>
              <th width:"10px">NF-e</th>
              <th>Peso</th>
              <th>Volume</th>
              <th>Status</th>
            </tr>
          </thead>

          <tbody>
            <% i = 1 %>
            <% @ordem_services.each do |ordem_service| %>
              <tr>
                <td>
                  <%= check_box_tag "os[ids][#{ordem_service.id}]", ordem_service.id, false, value: ordem_service.peso, onchange:"checkPesoTotal();", class: "escolha" %>
                </td>
                <td><%= link_to ordem_service.id, ordem_service %></td>
                <td><%= link_to truncate(ordem_service.client.nome, length: 16), ordem_service %></td>
                <td><%= link_to ordem_service.client.cidade, ordem_service %></td>
                <td><%= link_to ordem_service.client.estado, ordem_service %></td>
                <td><div><%= link_to ordem_service.get_number_nfe, ordem_service if ordem_service.nfe_keys.present? %></div></td>
                <td align="right"><%= link_to number_to_currency(ordem_service.peso, precision: 3, unit: "", separator: ",", delimiter: "."), ordem_service if !ordem_service.peso.blank? %></td>
                <td align="right"><%= link_to number_to_currency(ordem_service.qtde_volume, precision: 3, unit: "", separator: ",", delimiter: "."), ordem_service if !ordem_service.peso.blank? %></td>
                <td><%= ordem_service.status_name %></td>
              </tr>
              <% i = i +1 %>
            <% end %>
   
            <tr>
              <td colspan="6"><strong>Total Selecionado</strong></td>
              <td align="right"><%= text_field_tag :peso_total, "", value: 0, disabled: true, size: 10, style: "text-align: Right"  %></td>
              <td colspan="4"><strong></strong></td>
            </tr>
   
        </table>
      </div>    
    </div>
  </div>

  <%= submit_tag("Gerar Embarque", class: "btn btn-primary") %> 

  <% end %>
</div>

<hr>

<script type="text/javascript">
Number.prototype.formatMoney = function(c, d, t){
var n = this, 
    c = isNaN(c = Math.abs(c)) ? 2 : c, 
    d = d == undefined ? "." : d, 
    t = t == undefined ? "," : t, 
    s = n < 0 ? "-" : "", 
    i = parseInt(n = Math.abs(+n || 0).toFixed(c)) + "", 
    j = (j = i.length) > 3 ? j % 3 : 0;
   return s + (j ? i.substr(0, j) + t : "") + i.substr(j).replace(/(\d{3})(?=\d)/g, "$1" + t) + (c ? d + Math.abs(n - i).toFixed(c).slice(2) : "");
 };



  // function checkTotal() {
  //   var elements = document.getElementsByClassName('escolha');
  //   var label = document.getElementById('valor_total');
  //   var sum = 0;
  //   // console.log(elements[i].value);
  //   for (i=0;i< elements.length;i++) {
  //     if (elements[i].checked) {
  //       sum = sum + parseFloat(elements[i].value);
  //       console.log(elements[i].value);
  //     }
  //   }
  //   document.getElementById('valor_total').value = 'R$ ' + sum.formatMoney(2, ',', '.');

  // }

  function checkPesoTotal() {
    var elements = document.getElementsByClassName('escolha');
    var label = document.getElementById('peso_total');
    var sum = 0;
    // console.log(elements[i].value);
    for (i=0;i< elements.length;i++) {
      if (elements[i].checked) {
        sum = sum + parseFloat(elements[i].value);
        console.log(elements[i].value);
      }
    }
    document.getElementById('peso_total').value = sum.formatMoney(3, ',', '.');

  }

</script>

<script type="text/javascript">
jQuery(function($) {
  // when the #country field changes
  $("#type_service_id").change(function() {
    // make a POST call and replace the content
    var conta = $('select#type_service_id :selected').val();
    if(conta == "") conta="0";
    jQuery.get('/ordem_service_to_type_service/' + conta, function(data){
        $("#ordem_service_type_service_id").html(data);
    })
    return false;
  });
})
</script>
